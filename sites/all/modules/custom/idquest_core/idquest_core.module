<?php

/**
 * @file
 * Main file of IDQuest Core.
 */

/**
 * Implements hook_custom_theme().
 */
function idquest_core_custom_theme() {
  if (arg(0) == 'node' && is_numeric(arg(1)) && in_array(arg(2), array('', 'statistics')))  {
    return 'idquest';
  }
}
